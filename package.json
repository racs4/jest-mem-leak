{
  "name": "jest-mem-leak",
  "version": "0.0.0",
  "main": "src",
  "private": true,
  "scripts": {
    "test": "mocha --check-leaks --jobs 1 --require ./test/setup.js --spec src/**/*.test.js --timeout 600000",
    "test-band": "node --expose-gc ./node_modules/.bin/mocha --check-leaks --jobs 1 --require ./test/setup.js --spec src/**/*.test.js --timeout 600000"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "mongodb-memory-server": "^8.13.0"
  }
}
